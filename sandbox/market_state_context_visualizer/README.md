# Market State Context Visualizer (moved)

This sandbox path is **deprecated**.

Canonical location:

- `apps/context_visualizer/`
- Public viewer: `apps/context_visualizer/public/`
- Generator: `apps/context_visualizer/generate_lifecycle_context_data.py`

Runtime / Make targets:

```bash
make context-visual-stack-start
make context-visual-stack-status
```

Viewer URL: http://127.0.0.1:8765/

Do not add new visual source here. Keep this directory only as a compatibility pointer.
