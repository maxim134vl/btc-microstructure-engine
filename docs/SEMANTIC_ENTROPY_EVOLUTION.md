# SEMANTIC ENTROPY EVOLUTION

**Status:** Phase 3B  
**Module:** `semantic_entropy_evolution.py`

---

## Comparison

Explicit divergence between `STOPPING_VOLUME` and `SELLING_CLIMAX` post-event entropy behavior.

| Field | Meaning |
|-------|---------|
| `entropy_decay_rate` | Relative entropy shift difference |
| `entropy_compression_duration` | Stabilization duration difference |
| `post_event_entropy_persistence` | Persistence difference |
| `entropy_recovery_velocity` | Recovery velocity difference |
| `entropy_divergence_score` | Aggregate divergence index |

## Expected Behavior

- **Absorption (STOPPING):** stabilization, compression, recovery
- **Capitulation (SELLING):** instability persistence, slower recovery
