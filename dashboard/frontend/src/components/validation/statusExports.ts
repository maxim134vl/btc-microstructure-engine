/**
 * Validation dashboard — status system entry point (Phase 3.9 adoption).
 * Import from here when replacing legacy validation status labels.
 */
export {
  StatusBadge,
  StatusCardAccent,
  StatusCardAccentFromStatus,
  StatusDot,
  StatusIndicator,
  VALIDATION_STATUS,
  translateValidationLevel,
  translateByDomain,
  translateOpsLevel,
  type ResolvedStatus,
  type StatusDomain,
  type StatusTone,
  type ValidationStatusKey,
} from "../status";
