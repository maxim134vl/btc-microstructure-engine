import { enCommon } from "./common";
import { enNav } from "./nav";
import { enShell } from "./shell";
import { enStatus } from "./status";
import { enOps } from "./ops";
import { enValidation } from "./validation";
import { enVisualCognition } from "./visualCognition";
import { enMarketIntelligence } from "./marketIntelligence";
import { enRuntimeActivity } from "./runtimeActivity";
import { enStage1 } from "./stage1";
import { enCognitiveReview } from "./cognitiveReview";
import { enMarketState } from "./marketState";

export const en = {
  common: enCommon,
  nav: enNav,
  shell: enShell,
  status: enStatus,
  ops: enOps,
  validation: enValidation,
  visualCognition: enVisualCognition,
  marketIntelligence: enMarketIntelligence,
  runtimeActivity: enRuntimeActivity,
  stage1: enStage1,
  cognitiveReview: enCognitiveReview,
  marketState: enMarketState,
};
