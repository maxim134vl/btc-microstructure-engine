# ============================================================================
# Production .env — minimal. Only these two knobs.
# Everything else is baked into deploy/docker-compose.prod.yml.
# ============================================================================

# Image tag pushed by scripts/build-and-push.sh
BTC_VERSION=1.0.0

# Host port for the observability UI (http://<host>:<HEALTH_API_PORT>)
HEALTH_API_PORT=8080
