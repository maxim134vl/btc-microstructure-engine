# Archive Removed

Deprecated mirror trees, backup engines, and inventory snapshots relocated during Phase 4A consolidation.

- `backups/` — legacy `*_backup*.py` engine copies
- `snapshots/` — inventory snapshot text files (planned)

**Do not execute from this directory.** Canonical runtime remains at repository root / `src/btc_ml/`.

Mirror tree `btc-microstructure-engine/` is marked deprecated; full mirror archive is scheduled in a follow-up commit.
